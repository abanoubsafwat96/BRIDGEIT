<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.safwat.abanoub.bridgeit">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/NoActionBar">
        <activity android:name=".LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <!-- <activity android:name=".StudentMainMenuFragment" /> -->

        <activity
            android:name=".BusActivity"
            android:label="@string/title_activity_bus" />
        <activity android:name=".AnnouncementsActivity" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".StudentCompetitionActivity" />
        <activity android:name=".GalleryActivity" />
        <activity android:name=".MainMenuActivity" />
        <activity android:name=".ComplaintsActivity" />
        <activity android:name=".ChooseUserActivity" />
        <activity android:name=".MessagesActivity" />
        <activity android:name=".DoctorReportsActivity" />
        <activity android:name=".DoctorReportsDetailedActivity" />
        <activity android:name=".ComplaintsDetailedActivity" />
        <activity android:name=".ReplysActivity" />
        <activity android:name=".GradesGraphActivity" />
        <activity android:name=".Grades1Activity" />
        <activity android:name=".Grades2Activity" />
        <activity android:name=".AddGradesActivity" />
        <activity android:name=".ChooseSubjectActivity" />
        <activity android:name=".TeacherCompetitionActivity" />
        <activity android:name=".OldCompetitions2Activity" />
        <activity android:name=".QuickCompetitions2Activity" />
        <activity android:name=".ChooseCompetitionActivity" />
        <activity android:name=".AddAnnouncementsActivity" />
        <activity android:name=".ChildActivity"></activity>
    </application>

</manifest>